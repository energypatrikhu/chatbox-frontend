export interface User {
	id: string;
	name: string;
	email: string;
}

export interface UserContact {
	id: string;
	name: string;
}

export interface UserGroup {
	id: number;
	type: string;
	name: string;
}
